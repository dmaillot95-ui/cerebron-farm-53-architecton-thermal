name: ARCHITECTON THERMAL validate
on:
  push:
  workflow_dispatch:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python tools/validate_config.py
      - run: python tests/test_thermal_mvp.py
      - uses: actions/upload-artifact@v4
        with:
          name: farm53-thermal-mvp-evidence
          path: artifacts/thermal_mvp.json
